# service/

The kuhu service itself — planned as Cloudflare Workers + D1, one PWA with a
posting face and a subscribing face, web push delivery, JSON + MQTT out.

Season 0: nothing here yet, on purpose. See [../docs/concept.md](../docs/concept.md).
